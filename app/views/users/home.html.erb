<% provide(:title, 'All Users') %>
<h1>All Users</h1>
<ul class="users">
  <% @users = User.all %>
  <% @users.each do |user| %>
    <li>     
      <%= link_to user.id, user.username %>
      <%= link_to user.username, user %>
      <%= user.email %>
  <% end %>
</ul>
