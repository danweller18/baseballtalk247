<% if user_signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/review_form' %>
      </section>
    </aside>
  </div>
<% else %>
  
  <p>
    This is the home page for Baseball Talk 24/7.
  </p>
  <%= link_to "Sign up now!", new_user_registration_path, class: "btn btn-large btn-primary" %>

<% end %>
